const custoProduto = 10;
const  valorVenda = 30;



if (custoProduto >= 0 && valorVenda >= 0) {
    custoProdutotal = custoProduto * 20/100;
    const Lucro = (valorVenda - custoProdutotal) * 1000;
    console.log(Lucro)
} else {
    console.log("Error, os valores não podem ser negativos");
}